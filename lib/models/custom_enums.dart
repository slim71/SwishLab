/// Which functionality triggered the navigation to the video details page
enum OriginFunc {
  side,
  front,
}

/// Enumeration depicting the type of assets that custom widgets can dynamically load
enum AssetType {
  icon,
  image,
  json,
  animation,
  gif,
}
